server {
    listen 80;
    server_name localhost;
    root /var/www/html;

    # Resolver DNS per risolvere i nomi dei servizi Docker
    resolver 127.0.0.11 valid=30s;

    # Frontend
    location / {
        root /var/www/html/frontend;
        index index.html;
        try_files $uri $uri/ =404;
    }

    # API con CORS
    location /api {
        root /var/www/html/api;
        
        # Gestisci le richieste OPTIONS PRIMA di tutto
        if ($request_method = 'OPTIONS') {
            add_header 'Access-Control-Allow-Origin' '*';
            add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS';
            add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization';
            add_header 'Access-Control-Max-Age' 86400;
            add_header 'Content-Length' 0;
            add_header 'Content-Type' 'text/plain';
            return 204;
        }
        
        # Headers CORS per tutte le altre richieste
        add_header 'Access-Control-Allow-Origin' '*' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, DELETE, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'Content-Type, Authorization' always;
        
        try_files $uri $uri/ /api/index.php$is_args$args;
        
        location ~ ^/api/.*\.php$ {
            fastcgi_split_path_info ^(/api)(/.*)$;
            # Usa una variabile per forzare la risoluzione DNS dinamica
            set $php_backend php:9000;
            fastcgi_pass $php_backend;
            fastcgi_index index.php;
            include fastcgi_params;
            fastcgi_param SCRIPT_FILENAME /var/www/html/backend/api/index.php;
            fastcgi_param PATH_INFO $fastcgi_path_info;
            
            # Timeout aumentati per debug
            fastcgi_read_timeout 300;
            fastcgi_connect_timeout 300;
        }
    }
}